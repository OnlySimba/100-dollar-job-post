import Link from "next/link";import { MapPin,Calendar } from "lucide-react";import { Job } from "@/lib/types";import { categoryIcons } from "@/lib/data";
export function JobCard({job}:{job:Job}){return <article className="job-card"><div className="job-art" style={{background:job.image}}><span>{categoryIcons[job.category]}</span><b>$100</b></div><div className="job-body"><div className="row"><span className="tag">{job.category}</span><span className={`status ${job.status.replace(" ","-").toLowerCase()}`}>{job.status}</span></div><h3>{job.title}</h3><p>{job.description.slice(0,92)}…</p><div className="meta"><span><MapPin size={16}/>{job.distance} mi · {job.zip}</span><span><Calendar size={16}/>{new Date(job.date+"T12:00").toLocaleDateString("en-US",{month:"short",day:"numeric"})}</span></div><Link href={`/jobs/${job.id}`} className="text-link">View job →</Link></div></article>}
