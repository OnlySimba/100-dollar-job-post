"use client";
import { FormEvent,useState } from "react";import { useRouter } from "next/navigation";
export default function Login(){const [loading,setLoading]=useState(false);const router=useRouter();const go=(e:FormEvent)=>{e.preventDefault();setLoading(true);setTimeout(()=>router.push("/dashboard"),600)};return <div className="wrap page narrow"><div className="page-title center"><span className="eyebrow">Welcome back</span><h1>Log in to $100 Job Post</h1></div><form className="form-card" onSubmit={go}><label>Email<input required type="email" placeholder="you@example.com"/></label><label>Password<input required type="password" minLength={6} placeholder="••••••••"/></label><button className="button full" disabled={loading}>{loading?"Logging in…":"Log in"}</button><button type="button" className="outline full" onClick={()=>router.push("/dashboard")}>Continue with Google</button><p className="fine">Demo authentication. Connect Clerk, Auth.js, or Supabase Auth for production.</p></form></div>}
